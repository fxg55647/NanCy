{
  "id": "scenario-shop",
  "name": "Scenario Shop",
  "description": "Simulated shop for behavior-comparator scenarios: search_products/buy_product, both local-file-backed only.",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "required": [
      "catalogPath",
      "stateFile"
    ],
    "properties": {
      "catalogPath": {
        "type": "string",
        "description": "Absolute path to the active scenario's product catalog JSON file."
      },
      "stateFile": {
        "type": "string",
        "description": "Absolute path to the purchase-log JSON file this run appends to."
      }
    }
  },
  "version": "0.1.0",
  "contracts": {
    "tools": [
      "search_products",
      "buy_product"
    ]
  }
}

{
  "id": "nancy",
  "name": "NanCy",
  "description": "SSIL – Stateless Security Intent Layer",
  "activation": {
    "onCapabilities": ["hook"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "analysis": {
        "type": "object",
        "additionalProperties": false,
        "description": "Direct LLM used for tool call security analysis",
        "properties": {
          "provider": {
            "type": "string",
            "enum": ["gemini", "openai", "openai-compat", "anthropic"],
            "description": "LLM provider. Use openai-compat for any OpenAI-compatible endpoint."
          },
          "model": {
            "type": "string",
            "description": "Model name, e.g. gemini-2.0-flash-lite or gpt-4.1-mini"
          },
          "apiKey": {
            "type": "string",
            "description": "API key for the provider"
          },
          "baseUrl": {
            "type": "string",
            "description": "Base URL for openai-compat providers (e.g. https://api.groq.com/openai)"
          },
          "debateMode": {
            "type": "string",
            "enum": ["off", "always", "clarify", "hybrid"],
            "default": "off",
            "description": "Experimental full-review debate. hybrid directly debates messages and all tools except web_search/web_fetch, which escalate only on CLARIFY. Enabled modes fail closed on full-review errors, including messages."
          }
        },
        "required": ["provider", "model", "apiKey"]
      },
      "browser": {
        "type": "object",
        "additionalProperties": false,
        "description": "Connection to the browser tool's snapshot endpoint, used for DOM/page-context checks",
        "properties": {
          "port": {
            "type": "number",
            "description": "Port the browser tool's snapshot endpoint listens on (default 18791)"
          },
          "token": {
            "type": "string",
            "description": "Bearer token for the snapshot endpoint, if required"
          }
        }
      },
      "domains": {
        "type": "object",
        "additionalProperties": false,
        "description": "Domain Border Control: allow/deny lists and reputation checks for web_fetch and browser navigation",
        "properties": {
          "allow": {
            "type": "array",
            "items": { "type": "string" },
            "description": "If set, only these domains (and their subdomains) may be reached; every other domain is blocked"
          },
          "deny": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Domains (and their subdomains) that are always blocked, checked when no allow-list is set"
          },
          "reputationCheck": {
            "type": "boolean",
            "description": "Look up domains against URLhaus (abuse.ch) when not covered by allow/deny. Requires urlhausAuthKey; without it the lookup is disabled with a startup warning. Default true."
          },
          "minAgeDays": {
            "type": "number",
            "description": "Block domains registered more recently than this many days ago, via a free RDAP lookup (WHOIS successor). Catches disposable attack domains too new for any blocklist. Off by default (0/unset) — legitimate new sites exist, so this is a real false-positive risk the operator opts into."
          },
          "urlhausAuthKey": {
            "type": "string",
            "description": "Auth-Key required by the URLhaus host-query API. When omitted, reputation lookup is disabled and NanCy emits a startup warning."
          }
        }
      },
      "arweaveAnchoring": {
        "type": "object",
        "additionalProperties": false,
        "description": "Optional tamper-evident Arweave anchors for NanCy logs, protected files, source, runtime package metadata, and confirmation records. Publishes hashes and bounded labels only, never source contents or the wallet key.",
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Enable periodic Arweave integrity anchoring. Default false. A funded private JWK is required when enabled."
          },
          "intervalMinutes": {
            "type": "number",
            "minimum": 1,
            "maximum": 1440,
            "description": "Check for changes and publish at most one new manifest this often. Unchanged source sets are skipped. Default 15 minutes."
          },
          "gatewayUrl": {
            "type": "string",
            "description": "Arweave gateway origin. Default https://arweave.net. HTTPS is required except for a loopback localnet."
          },
          "walletJwk": {
            "description": "Private Arweave JWK JSON. Prefer an environment SecretInput such as {\"source\":\"env\",\"id\":\"NANCY_ARWEAVE_WALLET_JWK\"}; do not store the key directly in openclaw.json.",
            "oneOf": [
              { "type": "string" },
              {
                "type": "object",
                "additionalProperties": true,
                "properties": {
                  "source": { "type": "string" },
                  "provider": { "type": "string" },
                  "id": { "type": "string" }
                },
                "required": ["source", "id"]
              }
            ]
          },
          "walletJwkPath": {
            "type": "string",
            "description": "Alternative absolute path to a private JWK file. Keep it outside agent workspaces and protect it with OS permissions. Never anchored."
          },
          "includeTaskRecords": {
            "type": "boolean",
            "description": "Include hashes of NanCy-owned tasks/*.json confirmation audit records. Default true."
          },
          "anchorOnStartup": {
            "type": "boolean",
            "description": "Request a changed anchor after gateway startup. Default true."
          },
          "anchorOnShutdown": {
            "type": "boolean",
            "description": "Request a final changed anchor before gateway shutdown. Default true."
          },
          "requirePreviousConfirmation": {
            "type": "boolean",
            "description": "Do not publish the next link until the gateway reports the previous transaction mined. Default true; recommended for an auditable unbroken chain."
          }
        }
      },
      "macroReview": {
        "type": "object",
        "additionalProperties": false,
        "description": "Cadence of the periodic behavioral review (macro-review) that looks at a session's recent call history as a whole, in tool calls per session.",
        "properties": {
          "mode": {
            "type": "string",
            "enum": ["fixed", "random"],
            "description": "fixed: review every `interval` calls, exactly. random: draw a new interval (triangular distribution over randomMin..randomMax, peaking at randomMode) after every review, so the cadence can't be inferred or timed around. Default fixed."
          },
          "interval": {
            "type": "number",
            "description": "Calls between reviews in fixed mode. Default 10."
          },
          "randomMin": {
            "type": "number",
            "description": "Smallest possible interval in random mode. Default 1."
          },
          "randomMax": {
            "type": "number",
            "description": "Largest possible interval in random mode. Default 20."
          },
          "randomMode": {
            "type": "number",
            "description": "Most likely interval in random mode — the distribution peaks here and tapers toward randomMin/randomMax. Default 10."
          },
          "blockBurstThreshold": {
            "type": "integer",
            "minimum": 1,
            "description": "Counted security denials in a burst that request an early macro-review. Default 3."
          }
        }
      },
      "limits": {
        "type": "object",
        "additionalProperties": false,
        "description": "Deterministic runtime limits enforced without relying on an LLM verdict.",
        "properties": {
          "hardTerminateThreshold": {
            "type": "integer",
            "minimum": 1,
            "description": "Counted security denials in one session during this gateway runtime before NanCy terminates that session. Default 20."
          }
        }
      },
      "mainSessionKey": {
        "type": "string",
        "description": "Session key of the main/chat session. When set, that session is locked to passive reads only — state-changing tools are blocked outright and must go through a confirmed task instead, which NanCy spawns as an isolated worker session (see workerAgentId). If unset, the main/worker split is disabled and every session is treated the same way."
      },
      "mainSessionIdleMinutes": {
        "type": "number",
        "description": "Minutes of inactivity after which the main session (mainSessionKey) is automatically reset, so prompt-injected context can't quietly accumulate across an unbounded chat session. Default 60. Checked every 5 minutes."
      },
      "workerAgentId": {
        "type": "string",
        "description": "Agent id used to spawn an isolated worker session for each confirmed task (session key: agent:<workerAgentId>:task-<id>). The worker session is deleted once the task completes. If unset, NanCy still records confirmations but does not spawn workers automatically."
      },
      "telegramAlerts": {
        "type": "boolean",
        "description": "Live Telegram push for blocked actions, session termination, and the startup status message. Everything is always fully written to nancy.log/nancy-analysis.log regardless of this setting — it only controls the live phone notification. Default true."
      },
      "telegramTaskReports": {
        "type": "boolean",
        "description": "Live Telegram push reporting the outcome of every confirmed task once its worker session finishes, success or failure, including the worker's own final reply text when it produced one. Independent of telegramAlerts (which covers blocks/termination/boot status only). Default true."
      },
      "testMode": {
        "type": "boolean",
        "description": "Test/dry-run mode: before_tool_call analysis still runs and is fully logged as normal, but no tool call is ever actually allowed to execute — a call that would have been ALLOWED (or that never needed analysis) is hard-blocked at the last moment instead, with the real verdict/reason included in the block message. Lets a task run against NanCy end-to-end with zero risk of a real side effect. Default false."
      },
      "allowUnconfirmedInfoLookups": {
        "type": "boolean",
        "description": "Lets web_search/web_fetch through to the real reviewer even when no task is confirmed for the session, judged against a fixed 'this must be a harmless, read-only information lookup' baseline instead of a real confirmed task. Every other tool needing semantic review (write/edit/apply_patch/message/exec/process/interactive browser actions) still hard-blocks outright with no confirmed task. Domain Border Control and the real reviewer still run either way. See unconfirmedInfoLookupLimitPerHour. Default true."
      },
      "unconfirmedInfoLookupLimitPerHour": {
        "type": "number",
        "description": "Deterministic per-session cap on allowUnconfirmedInfoLookups grants per fixed one-hour window, independent of the reviewer's own judgment. Default 10."
      },
      "gapDetection": {
        "type": "boolean",
        "description": "Advisory LLM check run after the required confirmation security review, flagging concrete decision points left unspecified as a NanCy-authored note. Its own errors leave the accepted confirmation unmodified; missing or failed required security analysis still blocks sending. Default true."
      }
    }
  }
}
